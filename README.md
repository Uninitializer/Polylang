# ⚡ PolyLang ∞ — Python → 43 языка

**Транспайлер из Python в 43 языка программирования. Работает прямо в браузере.**

## 🚀 ВОЗМОЖНОСТИ

| Категория | Поддержка |
|-----------|-----------|
| **Асинхронность** | `async`/`await`, `async with`, `async for` |
| **Генераторы** | `yield`, `yield from`, асинхронные генераторы |
| **Паттерн-матчинг** | `match`/`case` с OR (`case 1 \| 2`) и guards (`case _ if x > 0`) |
| **Классы** | Наследование, `__slots__`, `@dataclass`, `@final`, `@abstract`, трейты, примеси |
| **Декораторы** | `@property`, `@staticmethod`, `@classmethod`, `@abstractmethod` |
| **Контекстные менеджеры** | `with`, `async with` |
| **Обработка ошибок** | `try`/`except`/`else`/`finally`, `raise` |
| **Комплехеншны** | list/dict/set comprehensions, генераторные выражения |
| **Ассемблер** | `asm { ... }`, `__asm__("...")` |
| **Верификация** | `requires`, `ensures`, `invariant` |
| **Транзакционная память** | `atomic { ... }`, `transaction { ... }` |

## 🌐 ЦЕЛЕВЫЕ ЯЗЫКИ (43)

<details>
<summary>Полный список</summary>

Python, JavaScript, TypeScript, Go, Rust, C, C++, Java, Kotlin, C#, Swift, Ruby, Lua, PHP, Perl, R, Zig, Nim, Dart, Scala, Groovy, Clojure, F#, Objective-C, Haskell, Elixir, OCaml, Julia, MATLAB, Fortran, COBOL, Pascal, Ada, Verilog, VHDL, Lisp, Scheme, Prolog, Erlang, Crystal, Chapel, Pony, Red

</details>

## 🎮 ПОПРОБОВАТЬ ОНЛАЙН

👉 **[Открыть PolyLang ∞](https://uninitializer.github.io/Polylang/)** 👈

## 📦 ЗАПУСК ЛОКАЛЬНО

```bash
git clone https://github.com/uninitializer/polylang.git
cd polylang
open index.html
